local minianimate = require("mini.animate")

minianimate.setup({
    resize = {
        enable = false
    },
    open = {
        enable = false
    },
    close = {
        enable = false
    },
})
